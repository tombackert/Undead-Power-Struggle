package tnt.model;

/**
 * Example model class which should be replaced with your actual implementation.
 */
public class Example {
    private int bar;

    /**
     * Create a new Example instance. This does nothing useful.
     *
     * @param bar A random value without any actucal semantics.
     */
    public Example(int bar) {
        this.bar = bar;
    }

    /**
     * Performs a nonsense arithmetic calculation using the given parameter and the private field.
     *
     * @param a A random value without any actual semantics.
     * @return The result of the nonsense arithmetic calculation
     */
    public int foo(int a) {
        return bar + a + 7;
    }
}
